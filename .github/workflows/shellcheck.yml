name: shellcheck

on:
  push:
  workflow_dispatch:

jobs:
  shellcheck:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v3

      - name: install
        run: sudo snap install --channel=edge shellcheck

      - name: check
        run: ./bin/shellcheck
